#!/bin/sh

set -ex

up=`git name-rev '@{u}'|cut -f2 -d' '`

if [ -n "$up" ]; then
    [ `git rev-list --count "$up"..HEAD` -gt 0 ] && \
        git rebase -i "$up"
fi

git fetch
git rebase origin/main
