[general]
accounts = gmail

[Account gmail]
localrepository = Local
remoterepository = Remote
synclabels = yes
labelsheader = X-Keywords
postsynchook = ~/bin/biff-gmail

[Repository Local]
type = GmailMaildir
localfolders = ~/Maildir

[Repository Remote]
type = Gmail
# brew install openssl
sslcacertfile = /usr/local/etc/openssl/cert.pem 
ssl = yes
ssl_version = tls1_2
remoteuser =
remotepass =
maxconnections = 1
folderfilter = lambda f: f in ['[Gmail]/All Mail', '[Gmail]/Drafts']

# Local Variables:
# mode: python
# End:

